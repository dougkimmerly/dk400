# DK/400 Web Terminal Dependencies

# Web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Database
psycopg2-binary>=2.9.0

# In-process scheduler (for Heroku/serverless)
apscheduler>=3.10.0
ntplib>=0.4.0

# Background jobs (optional - for full dk400 with Redis)
celery>=5.3.0
redis>=5.0.0

# CLI (optional)
textual>=0.50.0
httpx>=0.27.0

# Fixer - Issue tracking and remediation (optional)
anthropic>=0.18.0
asyncpg>=0.29.0
pyyaml>=6.0
docker>=7.0.0
paramiko>=3.4.0
aiohttp>=3.9.0
pynetbox>=7.3.0
