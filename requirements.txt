# dk400 platform dependencies
# Deployment-specific deps go in the deployment's requirements.txt

# Core
asyncpg>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
psycopg2-binary>=2.9.0

# Robot (Celery)
celery[redis]>=5.3.0
redis>=5.0.0

# API (FastAPI)
fastapi>=0.109.0
uvicorn>=0.27.0
websockets>=12.0

# Web UI scheduler
apscheduler>=3.10.0
ntplib>=0.4.0

# Process management
supervisor>=4.2.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
